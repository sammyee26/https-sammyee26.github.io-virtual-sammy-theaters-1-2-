// Virtual Sammy Theaters 1.2 public configuration.
// Keep secret API keys OUT of this file.
window.VST12_CONFIG = window.VST12_CONFIG || {
  // If the whole site is deployed on a host that also serves /api/outpaint,
  // leave this blank. GitHub Pages users can paste the deployed backend URL
  // inside the in-app AI Outpaint Studio instead.
  outpaintApiUrl: ""
};
